{
  "name": "librus-api-rewrited",
  "version": "1.0.0",
  "description": "api for librus",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api",
    "librus"
  ],
  "author": "kabaraniak",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
